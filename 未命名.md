~~~markdown
# 个人导航页改版提示词：苹果风深色玻璃拟态导航首页

你现在需要基于我现有的 **Vue 3 + Element Plus** 项目，重新设计并实现一个「个人导航页」。

目标风格参考：**苹果风 / macOS 风 / iCloud Dashboard 风格 / 深色玻璃拟态**。

请不要做成传统后台管理系统页面，而是做成一个高级、干净、现代、有质感的个人导航首页。

---

## 一、整体设计目标

请将当前页面升级为一个：

- 深色主题
- 苹果风格
- 玻璃拟态
- 蓝色渐变背景
- 卡片式布局
- 高级、简洁、通透
- 适合作为个人导航首页 / 工作台首页

的现代化页面。

整体视觉应该接近：

- macOS 深色模式
- iCloud 网页应用的卡片质感
- 苹果官网的简洁排版
- 半透明毛玻璃面板
- 柔和蓝色光晕背景

不要使用过重的赛博朋克风，也不要使用太强的霓虹灯效果。
整体要克制、精致、干净。

---

## 二、技术要求

项目当前技术栈为：

```text
Vue 3
Element Plus
Vite
SCSS / CSS
~~~

请在现有技术栈基础上实现，不要更换 UI 框架。

Element Plus 可以用于：

```text
下拉菜单
按钮
弹窗
Tooltip
表单
图标
```

但页面主体样式请尽量使用自定义 CSS / SCSS 实现，不要完全依赖 `el-card` 的默认样式。

------

## 三、页面整体布局

页面为桌面端宽屏布局，整体结构如下：

```text
页面容器
├── 顶部导航栏
├── 主视觉欢迎区
│   ├── 欢迎语
│   ├── 副标题
│   └── 搜索栏
├── 快捷入口卡片区
└── 底部信息面板区
    ├── 最近访问
    ├── 常用分类
    ├── 今日概览
    ├── 待办 / 日程
    └── 灵感便签
```

页面整体需要居中显示，最大宽度控制在 `1500px ~ 1600px` 左右。

------

## 四、背景设计

背景不要纯黑。

请使用深蓝色到午夜蓝的渐变背景，并加入类似 macOS 壁纸的柔和流线光效。

背景风格要求：

- 深蓝 / 靛蓝 / 黑蓝渐变
- 有柔和的蓝色流动光带
- 有轻微的径向光晕
- 不要太花
- 不要影响文字可读性
- 背景层级应该在内容下方

可以使用 CSS 实现：

```css
background:
  radial-gradient(circle at 50% 0%, rgba(70, 130, 255, 0.35), transparent 35%),
  radial-gradient(circle at 20% 30%, rgba(56, 189, 248, 0.18), transparent 30%),
  linear-gradient(135deg, #020617 0%, #061b3a 45%, #020617 100%);
```

可以额外增加几个模糊光斑：

```text
蓝色光斑
青色光斑
紫色光斑
```

使用 `filter: blur()` 和 `opacity` 做柔和效果。

------

## 五、顶部导航栏

顶部导航栏要求：

- 高度约 `64px`
- 固定在页面顶部区域，不需要吸顶
- 半透明玻璃质感
- 底部有一条很细的半透明边框
- 左侧显示应用图标和标题
- 右侧显示功能图标和用户信息

左侧内容：

```text
图标：一个简洁的蓝色圆角图标
标题：个人导航
```

右侧内容：

```text
搜索图标
通知图标
主题切换图标
用户头像 A
用户名 admin
下拉箭头
```

右侧用户区域使用圆角胶囊样式。

顶部栏样式参考：

```css
.header {
  height: 64px;
  backdrop-filter: blur(24px);
  background: rgba(7, 18, 38, 0.55);
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
}
```

------

## 六、主视觉欢迎区

页面中上方显示欢迎区。

内容示例：

```text
欢迎回来，admin 👋
高效连接你的工具、资源与灵感，开启专注的一天。
```

设计要求：

- 欢迎语居中
- 字体大、清晰、有层级
- 欢迎语颜色为白色
- 副标题为浅灰蓝色
- 与搜索栏之间保持舒适间距

建议样式：

```css
.hero-title {
  font-size: 42px;
  font-weight: 700;
  letter-spacing: -0.04em;
  color: rgba(255, 255, 255, 0.96);
}

.hero-subtitle {
  margin-top: 12px;
  font-size: 16px;
  color: rgba(220, 230, 255, 0.68);
}
```

------

## 七、搜索栏设计

搜索栏是页面核心组件。

要求：

- 居中显示
- 宽度约 `760px ~ 820px`
- 高度约 `58px ~ 64px`
- 圆角大
- 毛玻璃质感
- 边框有轻微蓝色高光
- 聚焦时有柔和蓝色光晕
- 左侧搜索图标
- 中间输入框
- 右侧快捷键提示 `⌘ K`
- 最右侧圆形箭头按钮

placeholder：

```text
搜索常用网站、工具或内容
```

搜索栏样式：

```css
.search-box {
  width: min(820px, 90vw);
  height: 64px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.08);
  border: 1px solid rgba(160, 190, 255, 0.35);
  backdrop-filter: blur(28px);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.16),
    0 20px 60px rgba(30, 100, 255, 0.22);
}
```

按钮风格：

```css
.search-action {
  width: 44px;
  height: 44px;
  border-radius: 50%;
  background: linear-gradient(135deg, #5ea0ff, #2563eb);
  box-shadow: 0 10px 30px rgba(37, 99, 235, 0.45);
}
```

------

## 八、快捷入口卡片区

搜索栏下方放置快捷入口卡片。

快捷入口建议 8 个：

```text
工作台
开发文档
服务器
代码仓库
常用网站
AI 助手
日程待办
收藏夹
```

每个卡片包含：

```text
图标
标题
描述
箭头
```

示例：

```text
工作台
进入统一工作台
处理日常事务
开发文档
查询技术文档与
开发指南
服务器
管理与监控
我的服务器
代码仓库
访问 Git 仓库与
代码资源
```

卡片设计要求：

- 横向排列，一行 8 个
- 使用 grid 布局
- 卡片为半透明玻璃质感
- 圆角 `18px ~ 22px`
- 边框为浅色透明线
- hover 时轻微上浮
- hover 时边框和背景稍微变亮
- 图标颜色可以丰富，但整体不要杂乱

卡片样式参考：

```css
.shortcut-card {
  min-height: 140px;
  padding: 22px;
  border-radius: 22px;
  background: rgba(255, 255, 255, 0.075);
  border: 1px solid rgba(255, 255, 255, 0.12);
  backdrop-filter: blur(24px);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.12),
    0 16px 40px rgba(0, 0, 0, 0.18);
  transition: all 0.25s ease;
}

.shortcut-card:hover {
  transform: translateY(-4px);
  background: rgba(255, 255, 255, 0.11);
  border-color: rgba(160, 190, 255, 0.35);
}
```

图标建议使用圆角方块承载：

```css
.shortcut-icon {
  width: 42px;
  height: 42px;
  border-radius: 12px;
}
```

------

## 九、底部信息面板区

快捷入口下方使用 dashboard widget 布局。

建议使用 CSS Grid：

```css
.dashboard-grid {
  display: grid;
  grid-template-columns: 1.1fr 0.85fr 0.9fr 1.05fr;
  gap: 18px;
}
```

页面底部包含以下模块。

------

### 1. 最近访问

标题：

```text
最近访问
```

右上角：

```text
查看全部
```

列表内容：

```text
GitHub        github.com              2 分钟前
Docker Hub    hub.docker.com          8 分钟前
Vercel        vercel.com              15 分钟前
Cloudflare    cloudflare.com          25 分钟前
Notion        notion.so               1 小时前
YouTube       youtube.com             2 小时前
```

要求：

- 左侧 favicon / 图标
- 中间网站名称和域名
- 右侧访问时间
- 行与行之间有非常淡的分割线
- 鼠标 hover 当前行时背景轻微变亮

------

### 2. 常用分类

标题：

```text
常用分类
```

右上角：

```text
...
```

分类示例：

```text
开发工具  12
效率工具  10
云服务    8
设计资源  9
娱乐休闲  5
学习平台  8
其他收藏  11
```

设计要求：

- 每行左侧有线性图标
- 右侧有数量 badge
- badge 使用半透明圆角胶囊
- 整体类似苹果设置页的列表风格

------

### 3. 今日概览

标题：

```text
今日概览
```

内容：

```text
75%
今日专注度
专注工作，效率真棒！
```

下面显示三个小统计：

```text
待办事项 12
日程安排 5
系统状态 正常
```

可以使用圆环进度条。

要求：

- 圆环使用蓝色渐变
- 中间显示百分比
- 统计卡片使用更小的玻璃卡片
- 系统状态“正常”使用绿色

------

### 4. 待办 / 日程

标题：

```text
待办 / 日程
```

右上角：

```text
查看全部
```

内容：

```text
完成项目需求文档      工作      今天 10:00
团队站会              工作      今天 14:00
学习 TypeScript 进阶  学习      明天 09:30
+ 添加待办
```

要求：

- 每条前面有 checkbox
- 标签使用小胶囊
- 时间右对齐
- `+ 添加待办` 放在底部

------

### 5. 灵感便签

标题：

```text
灵感便签
```

内容：

```text
好的设计是少即是多。
持续优化产品体验，创造价值。

今天 08:30
```

右侧可有一个小星标或固定图标。

便签卡片要求：

- 比普通面板更柔和
- 内部可以使用一块半透明渐变区域
- 有一点苹果备忘录 / 小组件的感觉

------

## 十、通用面板样式

所有底部 widget 面板使用统一样式。

```css
.glass-panel {
  border-radius: 24px;
  background: rgba(255, 255, 255, 0.075);
  border: 1px solid rgba(255, 255, 255, 0.11);
  backdrop-filter: blur(26px);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.12),
    0 20px 50px rgba(0, 0, 0, 0.22);
}
```

面板标题样式：

```css
.panel-title {
  font-size: 16px;
  font-weight: 600;
  color: rgba(255, 255, 255, 0.92);
}
```

辅助文字：

```css
.muted-text {
  color: rgba(220, 230, 255, 0.58);
}
```

------

## 十一、颜色规范

主背景色：

```text
#020617
#061b3a
#0f2a56
```

主强调色：

```text
#3b82f6
#60a5fa
#38bdf8
```

成功色：

```text
#34d399
```

警告色：

```text
#fbbf24
```

文本颜色：

```text
主文本：rgba(255,255,255,0.92)
副文本：rgba(220,230,255,0.65)
弱文本：rgba(220,230,255,0.45)
```

边框颜色：

```text
rgba(255,255,255,0.10)
rgba(160,190,255,0.25)
```

------

## 十二、交互效果

请补充基础交互效果：

### 卡片 hover

```text
轻微上浮
边框变亮
背景透明度提高
阴影增强
```

### 搜索框 focus

```text
蓝色外发光
边框变亮
```

### 按钮 hover

```text
亮度提高
轻微 scale
```

### 列表项 hover

```text
背景轻微变亮
```

------

## 十三、响应式要求

桌面端为主，但需要兼容较小屏幕。

建议断点：

```text
>= 1400px：完整布局
1024px - 1399px：快捷卡片每行 4 个
768px - 1023px：快捷卡片每行 2 个，底部面板两列
< 768px：单列布局
```

------

## 十四、Vue 组件拆分建议

请尽量拆分为以下组件：

```text
src/views/navigation/index.vue
src/views/navigation/components/NavHeader.vue
src/views/navigation/components/HeroSearch.vue
src/views/navigation/components/ShortcutGrid.vue
src/views/navigation/components/ShortcutCard.vue
src/views/navigation/components/RecentVisitPanel.vue
src/views/navigation/components/CategoryPanel.vue
src/views/navigation/components/OverviewPanel.vue
src/views/navigation/components/TodoSchedulePanel.vue
src/views/navigation/components/NotePanel.vue
src/views/navigation/components/DynamicBackground.vue
```

------

## 十五、数据结构建议

请先在页面中使用 mock 数据，方便后续接接口。

快捷入口数据：

```js
const shortcuts = [
  {
    title: '工作台',
    desc: '进入统一工作台\n处理日常事务',
    icon: 'Grid',
    color: 'blue',
    url: '#'
  },
  {
    title: '开发文档',
    desc: '查询技术文档与\n开发指南',
    icon: 'Document',
    color: 'green',
    url: '#'
  },
  {
    title: '服务器',
    desc: '管理与监控\n我的服务器',
    icon: 'Server',
    color: 'purple',
    url: '#'
  },
  {
    title: '代码仓库',
    desc: '访问 Git 仓库与\n代码资源',
    icon: 'Code',
    color: 'orange',
    url: '#'
  },
  {
    title: '常用网站',
    desc: '快速访问常用网站\n与在线服务',
    icon: 'Globe',
    color: 'cyan',
    url: '#'
  },
  {
    title: 'AI 助手',
    desc: '智能助手为你\n提供支持',
    icon: 'Robot',
    color: 'violet',
    url: '#'
  },
  {
    title: '日程待办',
    desc: '查看日程安排\n与待办事项',
    icon: 'Calendar',
    color: 'yellow',
    url: '#'
  },
  {
    title: '收藏夹',
    desc: '我的收藏与\n重要链接',
    icon: 'Star',
    color: 'green',
    url: '#'
  }
]
```

最近访问数据：

```js
const recentVisits = [
  { name: 'GitHub', domain: 'github.com', time: '2 分钟前' },
  { name: 'Docker Hub', domain: 'hub.docker.com', time: '8 分钟前' },
  { name: 'Vercel', domain: 'vercel.com', time: '15 分钟前' },
  { name: 'Cloudflare', domain: 'cloudflare.com', time: '25 分钟前' },
  { name: 'Notion', domain: 'notion.so', time: '1 小时前' },
  { name: 'YouTube', domain: 'youtube.com', time: '2 小时前' }
]
```

分类数据：

```js
const categories = [
  { name: '开发工具', count: 12 },
  { name: '效率工具', count: 10 },
  { name: '云服务', count: 8 },
  { name: '设计资源', count: 9 },
  { name: '娱乐休闲', count: 5 },
  { name: '学习平台', count: 8 },
  { name: '其他收藏', count: 11 }
]
```

待办数据：

```js
const todos = [
  { title: '完成项目需求文档', tag: '工作', time: '今天 10:00', done: false },
  { title: '团队站会', tag: '工作', time: '今天 14:00', done: false },
  { title: '学习 TypeScript 进阶', tag: '学习', time: '明天 09:30', done: false }
]
```

------

## 十六、实现注意事项

1. 页面不要出现明显的 Element Plus 默认后台风。
2. 不要使用纯黑背景。
3. 不要让卡片边框太亮。
4. 不要让文字过小。
5. 不要堆太多颜色。
6. 不要把页面做得太满，要有苹果风格的留白。
7. 图标可以使用 Element Plus Icons，也可以接入 `lucide-vue-next`。
8. 如果使用动态背景，先用 CSS 实现即可，不需要 Three.js。
9. 所有卡片和背景动画需要注意性能。
10. 整体要保持简洁、高级、统一。

------

## 十七、最终交付要求

请直接修改或新增相关 Vue 文件，实现完整页面。

最终页面需要达到以下效果：

```text
一个苹果风深色玻璃拟态个人导航首页
顶部有导航栏和用户信息
中间有欢迎语和高级搜索栏
下方有 8 个快捷入口卡片
底部有最近访问、常用分类、今日概览、待办日程、灵感便签等模块
整体蓝色深色背景，具有 macOS 风格的柔和流动光效
卡片有毛玻璃质感、细边框、圆角和轻微阴影
交互自然，有 hover 和 focus 效果

```
